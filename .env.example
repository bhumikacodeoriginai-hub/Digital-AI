# Add server-side AI and database credentials when connecting the production API.
VITE_API_BASE_URL=http://localhost:8000
VITE_APP_ENV=development
DATABASE_URL=postgresql://codeorigin:change-me-local-only@localhost:5432/learning_studio
REDIS_URL=redis://localhost:6379/0
SESSION_SECRET=replace-with-a-long-random-server-secret
# Never expose provider keys in VITE_* values. Keep those on the protected backend.
